# NOTE: These are placeholder values. Replace with actual secrets before deploying.
# In production, use sealed-secrets or external-secrets-operator.
apiVersion: v1
kind: Secret
metadata:
  name: blog-secrets
  namespace: blog-service
type: Opaque
stringData:
  DATABASE_URL: "postgres://user:password@postgres-host:5432/blogdb?sslmode=require"
  JWT_SECRET: "your-super-secret-jwt-key-minimum-32-characters-long"
